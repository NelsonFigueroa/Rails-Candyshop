<h2>Delete shop?</h2>

<%= link_to("<< Back to Shops List", shops_path, :class => 'back-link') %>

<br><br>

<%= form_for(@shop, :method => 'delete') do |f| %>

<p>Permanently delete this shop? : <%= f.object.name %></p>
<p>This will also delete all associated shelves/candies</p>

<%= f.submit("Delete Shop") %>
<% end %>
