<h2>Delete shelf?</h2>

<%= link_to("<< Back to Shop", shop_path(:id => @shop.id), :class => 'back-link') %>

<br><br>

<%= form_for(@shelf, :url => shelf_path(:shop_id => @shop.id), :method => 'delete') do |f| %>

<p>Permanently delete Shelf with ID <%= f.object.id %>? Candies will be moved to unshelved.</p>

<%= f.submit("Delete Shelf") %>
<% end %>
